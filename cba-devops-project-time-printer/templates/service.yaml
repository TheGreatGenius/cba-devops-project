apiVersion: v1
kind: Service
metadata:
  name: {{ include "cba-devops-project-time-printer.fullname" . }}
  labels:
    {{- include "cba-devops-project-time-printer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cba-devops-project-time-printer.selectorLabels" . | nindent 4 }}
